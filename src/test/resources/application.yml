spring:
  datasource:
    url: jdbc:h2:file:./data/muma-test
  jpa:
    hibernate:
      ddl-auto: update

spotify:
  base-url: https://api.spotify.com/v1
  client-id: test-client-id
  client-secret: test-client-secret
